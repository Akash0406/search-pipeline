/**
 * `@careerstack/auth` — framework-agnostic auth core: sessions, token hashing,
 * CSRF, OAuth + PKCE, and magic-link token generation/verification.
 *
 * Implemented in a later task; this placeholder keeps the package valid.
 */
export const AUTH_PACKAGE = '@careerstack/auth' as const;
