/**
 * `@careerstack/testing` — fast-check arbitrary factories, fixture loader and a
 * shared test harness (100-run default for property tests).
 *
 * Implemented in a later task; this placeholder keeps the package valid.
 */
export const TESTING_PACKAGE = '@careerstack/testing' as const;
