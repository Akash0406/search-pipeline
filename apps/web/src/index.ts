/**
 * `@careerstack/web` — Next.js App Router frontend (public surface + app shell).
 *
 * The Next.js application is set up in a later task; this placeholder keeps the
 * app a valid, type-checkable member of the workspace.
 */
export const WEB_APP = '@careerstack/web' as const;
