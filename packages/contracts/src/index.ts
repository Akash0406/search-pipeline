/**
 * `@careerstack/contracts` — shared Zod request/response schemas and the
 * generated OpenAPI types (the single source of truth for API I/O).
 *
 * Implemented in a later task; this placeholder keeps the package valid.
 */
export const CONTRACTS_PACKAGE = '@careerstack/contracts' as const;
