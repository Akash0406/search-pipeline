/**
 * `@careerstack/worker` — BullMQ consumers running the ingestion pipeline,
 * retention and expiry jobs.
 *
 * The worker bootstrap is set up in a later task; this placeholder keeps the app
 * a valid, type-checkable member of the workspace.
 */
export const WORKER_APP = '@careerstack/worker' as const;
