/**
 * `@careerstack/ui` — shared React component library (design system built on
 * shadcn/ui + Tailwind).
 *
 * Components arrive in later tasks; this placeholder keeps the package valid.
 */
export const UI_PACKAGE = '@careerstack/ui' as const;
