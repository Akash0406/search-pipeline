/**
 * `@careerstack/config` — typed configuration loader.
 *
 * The typed loader (brandName, retention, rate limits, fetch bounds, OAuth /
 * magic-link settings) is implemented in a later task. This placeholder keeps
 * the package a valid, type-checkable, buildable member of the workspace.
 */
export const CONFIG_PACKAGE = '@careerstack/config' as const;
