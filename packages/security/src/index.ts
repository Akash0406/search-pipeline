/**
 * `@careerstack/security` — SafeFetcher, SSRF guard, HTML sanitizer and
 * canonical URL helpers (the single outbound-HTTP chokepoint).
 *
 * Implemented in a later task; this placeholder keeps the package valid.
 */
export const SECURITY_PACKAGE = '@careerstack/security' as const;
