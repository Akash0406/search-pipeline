/**
 * `@careerstack/observability` — structured logging, tracing/metrics hooks and
 * correlation-id propagation shared by the API and worker.
 *
 * Implemented in a later task; this placeholder keeps the package valid.
 */
export const OBSERVABILITY_PACKAGE = '@careerstack/observability' as const;
